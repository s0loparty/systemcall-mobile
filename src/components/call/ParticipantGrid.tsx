import React from 'react';import {FlatList,StyleSheet,Text,View} from 'react-native';import {isTrackReference,useTracks,VideoTrack,type TrackReferenceOrPlaceholder} from '@livekit/react-native';import {Track} from 'livekit-client';
export function ParticipantGrid(){const tracks=useTracks([{source:Track.Source.Camera,withPlaceholder:true}],{onlySubscribed:false});const remote=tracks.filter(t=>t.participant&&!t.participant.isLocal);if(!remote.length)return <View style={s.empty}><Text style={s.emptyText}>Ожидаем участников…</Text></View>;return <FlatList data={remote} keyExtractor={(x,i)=>`${x.participant.identity}:${x.source}:${i}`} renderItem={({item})=><Tile trackRef={item}/>}/>}
function Tile({trackRef}:{trackRef:TrackReferenceOrPlaceholder}){return <View style={s.tile}>{isTrackReference(trackRef)?<VideoTrack trackRef={trackRef} style={s.video}/>:<View style={s.empty}><Text style={s.name}>{trackRef.participant.name||'Участник'}</Text></View>}</View>}
const s=StyleSheet.create({tile:{minHeight:250,flex:1,overflow:'hidden',borderRadius:24,backgroundColor:'#181818',marginBottom:10},video:{flex:1},empty:{flex:1,alignItems:'center',justifyContent:'center'},emptyText:{color:'#8b8b8b'},name:{color:'#fff',fontSize:18,fontWeight:'700'}});

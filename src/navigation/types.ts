import type {LiveKitCredentials} from '../types/api';
export type RootStackParamList={Home:undefined;PreJoin:{publicId:string;roomName:string};Call:{roomName:string;livekit:LiveKitCredentials;cameraEnabled:boolean;microphoneEnabled:boolean}};
